name: Check Version

on:
  pull_request:
    branches: [ "master" ]

jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Get version
      run: echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV
    - name: Print the version
      run: echo "The version we just got is ${{ VERSION }}"
